{
  "experimental": {
    "modelContextProtocolServers": [
      {
        "transport": {
          "type": "stdio",
          "command": "uvx",
          "args": ["sol-agent-wallet"]
        }
      },
      {
        "_comment": "write mode template — copy into the array above and fill SOLANA_PRIVATE_KEY",
        "transport": {
          "type": "stdio",
          "command": "uvx",
          "args": ["sol-agent-wallet"],
          "env": {
            "SOLANA_PRIVATE_KEY": "PASTE_BURNER_WALLET_BASE58_KEY_HERE",
            "SOLANA_NETWORK": "devnet",
            "SOLANA_MAX_TX_SOL": "1.0"
          }
        }
      }
    ]
  }
}
